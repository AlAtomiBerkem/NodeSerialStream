# Briefing App: Музей ТУ-144

Интерактивное приложение Инструктаж и маршрут экспозиции для посетителей Музея ТУ-144.

## 🚀 Основная идея и принцип работы
Приложение разработано для сенсорных экранов с разрешением 1080x1920 (портретный режим). Основная задача — предоставить посетителю удобный интерфейс перед входом в фюзеляж самолета.

### Навигационная логика:
1.  **Главный экран (`MainPage`)**: Центральный узел навигации. Содержит две крупные кнопки-ссылки на основные разделы.
2.  **Инструктаж (`Instructions`)**: Список правил поведения на борту. Реализован в виде списка аккордеонов для экономии места.
3.  **Маршрут экспозиции (`ExhibitionRoute`)**: Интерактивная карта самолета. Это ключевой элемент, где реализована двусторонняя связь между точками на карте и описанием зон.

## 🛠 Технологический стек
- **React 19**: Основа интерфейса.
- **TypeScript**: Строгая типизация для стабильности кода.
- **Vite**: Сборка и быстрый HMR.
- **Redux Toolkit**: Управление состоянием (синхронизация карты и списка).
- **SCSS Modules**: Изолированные стили компонентов.
- **React Router 7**: Навигация без перезагрузки страницы.

## 🏗 Архитектура (FSD)
Проект следует принципам **Feature-Sliced Design**:

### 1. `src/app` (Глобальные настройки)
- `provider`: Настройка Redux Store (`store.ts`) и слайсов (`btnSlice.ts`).
- `styles`: Глобальные стили, сброс (`reset.css`) и шрифты (`Acrobat`).

### 2. `src/pages` (Экраны приложения)
- `MainPage`: Приветствие и выбор раздела. Использует SVG-кнопки для переходов.
- `Instructions`: Страница с аккордеоном правил. Фиксированный заголовок и прокручиваемый список.
- `ExhibitionRoute`: Сложный экран с абсолютным позиционированием элементов карты.

### 3. `src/widgets` (Сложные блоки)
- **`AccordionGroup`**: Умный контейнер для аккордеонов. Он слушает состояние Redux (`activeButton`) и автоматически раскрывает нужный пункт, если пользователь нажал на метку на карте.
- **`BackgroundScreen`**: Обертка для всех страниц, обеспечивающая стабильный фон и центрирование контента.

### 4. `src/shared` (Примитивы и ресурсы)
- **`ui`**:
  - `DiamondBtn`: Кнопка-ромбик. Является атомарным компонентом. В `ExhibitionRoute` используется и как интерактивная метка на карте, и как иконка в заголовке аккордеона.
  - `BackButton`: Компонент возврата на предыдущую страницу. Внутри использует `Link` или программную навигацию через `useNavigate`.
- **`lib`**: Утилиты, такие как `classNames` для удобной работы с CSS-классами.
- **`assets`**: Все SVG-ресурсы, иконки и шрифты.

## 🎯 Ответственность компонентов (Кто за что отвечает)

### `BackgroundScreen` (Widget)
Главная обертка для всех страниц. Она гарантирует, что контент всегда будет вписан в экран (1080x1920), управляет фоновым изображением и предотвращает нежелательную прокрутку всей страницы.

### `AccordionGroup` (Widget)
«Мозг» списка описаний. Он не просто рендерит массив данных, но и синхронизирует их с глобальным стейтом. 
- **Роль**: Следить за `activeButton` в Redux и сообщать дочерним аккордеонам, кто из них должен быть открыт.

### `Acardion` (Widget/Component)
Отвечает исключительно за визуализацию и анимацию одного блока.
- **Роль**: Рассчитать `scrollHeight` своего контента и плавно раскрыться при получении пропса `isOpen`.

### `btnSlice` (Redux)
Единый источник истины для активных элементов.
- **Роль**: Хранить ID (например, "01", "05") текущей выбранной зоны. Это позволяет карте и списку «общаться» друг с другом, не зная о существовании друг друга напрямую.

## ⚙️ Функциональные особенности и элементы

### Синхронизация «Карта ↔ Список»
Это реализовано через Redux-слайс `btnSlice`. 
- Когда пользователь кликает на **`DiamondBtn`** на карте, вызывается экшен `activateButton`, который обновляет `activeButton` в сторе.
- **`AccordionGroup`** подписан на это изменение через `useSelector`. При получении нового ID он находит индекс соответствующего элемента и вызывает его раскрытие.
- Обратная связь: при клике на заголовок аккордеона, он также отправляет свой ID в Redux, чтобы метка на карте «загорелась».

### Плавные анимации
Аккордеоны используют JavaScript для вычисления `scrollHeight`. Это позволяет анимировать переход от `0` до `auto` (точная высота контента), что невозможно сделать чистым CSS без использования фиксированных `max-height`.

### Автовозврат на главный экран
Для обеспечения готовности киоска к новому посетителю реализована система отслеживания бездействия (`useIdleTimer`).
- **Таймаут**: 2 минуты (120 000 мс).
- **События**: Любое движение мыши, клик, нажатие клавиш или скролл сбрасывают таймер.
- **Действие**: Автоматический переход на главную страницу (`/`) по истечении времени.

## 📥 Развертывание и запуск

### Установка зависимостей
```bash
npm install
```

### Запуск в режиме разработки
```bash
npm run dev
```

### Сборка проекта
```bash
npm run build
```
