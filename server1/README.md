# Server 1: Центральный узел обработки событий

Основной бэкенд и панель управления пользователями и событиями музея.

## 🚀 Основная идея и принцип работы
Server 1 является связующим звеном между базой данных пользователей и внешними RSS-сервисами (`RssStart`, `RssExit`). Он координирует создание и удаление пользовательских сессий при взаимодействии с экспозицией.

## 🛠 Технологический стек
- **Node.js & Express**: Серверная логика.
- **MongoDB**: Основное хранилище данных.
- **Docker**: Контейнеризация для стабильного развертывания.
- **React (Client)**: Интерфейс администратора или мониторинга.

## ⚙️ Интеграции
Сервис активно взаимодействует со следующими URL (настраиваются через переменные окружения):
- `RSS_START_URL`: Отправка события активации.
- `RSS_EXIT_URL`: Отправка события завершения.

## 🏗 Архитектура
- `server/`: Реализация API и бизнес-логики.
- `client/`: Фронтенд для визуализации состояния системы.

## 📥 Запуск через Docker
Рекомендуется запускать через корневой `docker-compose.yml`.
```bash
docker-compose up server1
```
