# NFC Reader: Сервис интеграции с оборудованием

Служебный бэкенд-сервис для чтения данных с NFC-меток через последовательный порт (Serial Port).

## 🚀 Основная идея и принцип работы
Сервис слушает физический COM-порт, к которому подключен считыватель. При обнаружении метки он считывает её ID, сопоставляет с данными в MongoDB и отправляет уведомления другим частям системы через Socket.io или HTTP-запросы.

## 🛠 Технологический стек
- **Node.js**: Среда выполнения.
- **Express**: HTTP-сервер для API.
- **SerialPort**: Библиотека для связи с аппаратными устройствами.
- **Mongoose / MongoDB**: Хранение данных о метках и пользователях.
- **Socket.io**: Передача событий считывания в реальном времени.

## ⚙️ Функционал
- **serialPortSetup.js**: Конфигурация и автоматическое переподключение к порту.
- **routes.js**: API для управления базой данных меток.
- **db.js**: Логика подключения к базе данных.

## 📥 Запуск
```bash
npm install
npm run dev
```

> [!IMPORTANT]
> Для работы требуется физический считыватель, подключенный по USB/COM, или эмулятор порта.
