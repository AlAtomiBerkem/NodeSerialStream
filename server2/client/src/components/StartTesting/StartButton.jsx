import startButton from '../../UI/start buttons/startActive.svg'
import startButtonActive from '../../UI/start buttons/ startButtonActive.svg'

export const StartButton = () => {
    return(
        <>
        </>
    )
}

export default StartButton