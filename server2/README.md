# Server 2: Вспомогательный сервис данных

Дополнительный бэкенд для обеспечения работы клиентских приложений.

## 🚀 Основная идея и принцип работы
Server 2 предоставляет API для работы с дополнительными наборами данных, не входящими в зону ответственности Server 1. Он работает в паре со своим клиентским приложением (`client2`).

## 🛠 Технологический стек
- **Node.js**: Серверная часть.
- **MongoDB**: Общая база данных системы.
- **Docker**: Изоляция и деплой.

## 🏗 Структура
- `server/`: API эндпоинты.
- `client/`: Фронтенд-приложение (доступно на порту 3000 в Docker-сети).

## 📥 Запуск
Обычно запускается в составе общего стека:
```bash
docker-compose up server2 client2
```
